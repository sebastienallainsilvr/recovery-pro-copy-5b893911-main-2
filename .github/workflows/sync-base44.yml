name: Sync avec Base44
on:
  schedule:
    - cron: '0 */6 * * *'  # Toutes les 6 heures
  workflow_dispatch:  # Pour tester manuellement

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Sync repos
        run: |
          # Clone Base44
          git clone --bare https://github.com/base44dev/recovery-pro-copy-5b893911.git temp
          cd temp
          
          # Push vers votre repo (mise à jour)
          git push --mirror https://${{ secrets.GITHUB_TOKEN }}@github.com/sebastienallainsilvr/recovery-pro-copy-5b893911-main-2.git
